# PRD — 大学生日程助手（一期 · MVP）

> 版本：v1.0 ｜ 日期：2026-09-19 ｜ 阶段：Day 4 产品定义
> 文档关系：本 PRD 依据《大学生日程助手-设计方案.md》(v1.1) 与《research.md》起草，定义**28 天课程周期内要交付的一期核心子集**。范围冲突时以设计方案 v1.1 为准。
> 实现原则：规则优先，AI 兜底（一期核心闭环不依赖任何 AI 服务）。

---

## 一、产品概述

一款**大学生自用**的日程管理工具，网页版，手机与电脑均可访问。

核心主张：**把课程表和日常待办收进同一张时间轴**。现有的课表 App 只管上课、待办 App 只管任务，想知道「周四还有多少空档写作业」要开两个应用对着看——本工具用一套数据、一个周视图解决这件事。

一句话定位：**课表 + 待办同屏的轻量日程助手，先让工具帮用户「看见时间」，再逐步升级为「替用户安排时间」。**

## 二、目标用户

| 维度 | 描述 |
|---|---|
| 用户 | 开发者本人（大学生），单用户，无账号体系 |
| 设备 | 手机（日常查看、课前提醒）+ 电脑（录入与管理） |
| 痛点 | ① 课表归课表、待办归待办，两套账对不上；② 时间安排全靠手动，不够智能；③ 到点容易忘 |
| 使用频率 | 每天：早上看今日、课后打勾、晚上看结算；每周：录新课/新任务 |

## 三、核心场景

**场景 A · 周日晚录入（电脑端）**
下周一开学，我打开网页，用 5 分钟把新学期的课录进去：每门课填名称、星期、节次、周次规则、教室。录完切到周视图，一眼看到「哪天满、哪天空」。顺手把两条作业 deadline 也加成待办。

**场景 B · 工作日早上（手机端）**
早饭后打开网页，「今日视图」直接列出今天所有课和到期任务，底部的今日结算显示「今日 5 项，已完成 2」。上课前 15 分钟，手机系统日历弹出提醒（数据已导出 .ics 进系统日历）。

**场景 C · 冲突拦截（录入时）**
我想把一门讲座加在周三下午，但那个时段已有课——添加时工具当场提示冲突。我改到周四，顺利保存。

**场景 D · 换设备不丢数据（兜底）**
我在电脑上录了课表，想带到手机上用：从电脑端一键导出 JSON 文件，在手机端导入，数据完整迁移。（注意：本期**无云同步**，靠导出/导入迁移，这是用户已定的决策。）

## 四、功能清单

每个功能按「用户动作 → 可观察结果」描述。数据字段以设计方案第三节的 `schedules` 表为准，界面只开放本期所需字段。

### F1 学期设置
- **用户动作**：在设置页填写学期名、第一周周一的日期、总周数；并按自己学校的作息表逐节修改上课节次的起止时间（默认 10 节，可增可删，最多 15 节）。顶部有「每节时长 / 课间」辅助输入：**改这两个数，全表节次实时自动重排**（第 1 节开始不动，午休等大空档长度保留）；改某节「开始时间」后该节结束自动算出、后面各节整体顺移（特殊情况直接改「结束时间」即可，不会被顶掉）；「添加一节」的时间默认按上一节结束 + 课间算。
- **可观察结果**：保存后，周视图顶部显示「第 N 周」（按当前日期与开学日期换算）；周次换算符合单双周规则（第 1、3、5…周为单周）。节次时间保存后再打开设置页，看到的是自己改过的那些时间；把节次全部删掉则表示「不设置节次」，这时页面显示空节次并给出「恢复默认 10 节」按钮（Day 8 修正：早期版本会在这里把默认 10 节又显示回来，看着像没生效）。
- **说明（Day 8 增补，当晚更新）**：加节次时间是因为各校各专业作息不同，不能写死。节次时间已接上第一批消费者：**周视图与今日视图出现节次时间轴**，课程卡片按自己开始时间落到对应节次行上（跨节连堂占多行；开始时间不在任何节次内时就近落位，卡片仍显示真实时间）。还没接的另一半：添加课程时从节次反推时间。

### F2 手动添加课程
- **用户动作**：在周视图点「添加课程」，填写：课程名称（必填）、星期、节次/起止时间、周次规则（每周 / 单周 / 双周 三选一）、教室地点（选填）、备注（选填）。
- **可观察结果**：保存后课程立即出现在周视图对应星期与节次的格子里；格子内可见课程名与教室；按周次规则只在对应周显示（单周课在双周不出现）。

### F3 编辑与删除课程
- **用户动作**：点击周视图中的课程格子，进入编辑，修改任意字段或删除该课程。
- **可观察结果**：保存后周视图即时更新；删除后格子恢复空白。

### F4 时间冲突检测
- **用户动作**：添加或修改课程时，若时间与已有课程重叠。
- **可观察结果**：当场弹出提示，标明与哪门课冲突；用户可选择放弃或继续保存（提示但不强制阻止，与方案一致）。

### F5 添加待办
- **用户动作**：点「添加待办」，填写：标题（必填）、截止时间、备注（选填）。
- **可观察结果**：待办出现在周视图其截止日期对应列的待办区，以及今日视图与今日列表中；一条待办有标题、截止时间、完成状态三种可见信息。

### F6 完成打勾
- **用户动作**：点击待办前的勾选框。
- **可观察结果**：待办标记为完成（视觉上变灰/划线）；再次点击可取消完成。

### F7 今日结算
- **用户动作**：无需操作，打开今日视图即显示。
- **可观察结果**：页面底部显示「今日完成 N 项 / 共 M 项」（M 为当日**到期待办**总数，N 为其中已完成数；课程不计入，与 A7 一致）；数字随打勾实时变化。

### F8 周视图与今日视图
- **用户动作**：在两个视图间切换。
- **可观察结果**：周视图 = 一张周网格（行=节次，列=周一至周日；左侧有节次时间轴，课程卡片对齐到节次行），课程与待办同屏，能看出哪天空哪天满；今日视图 = 只列今天的内容，「今天的课」同样带节次轴。节次表被删空（不设置节次）时两个视图退回不画轴的旧布局。

### F9 数据导出（.ics 日历 / JSON 备份与迁移）
- **用户动作**：点「导出」，选择导出为 .ics 日历文件或 JSON 备份文件。
- **可观察结果**：.ics 导入手机系统日历后，每节课在**课前 15 分钟**生成系统提醒（提前量固定 15 分钟）；修改课程后需重新导出 .ics，系统日历的提醒才会同步更新。JSON 文件可在另一台设备的本工具中导入，数据完整还原。

### F10 本地保存
- **用户动作**：正常使用，无需操作。
- **可观察结果**：所有数据保存在本机浏览器中，**关闭浏览器重新打开、甚至重启电脑后数据仍在**；无网络时工具仍可使用。

### F11 响应式双端
- **用户动作**：分别用手机和电脑浏览器打开同一网址。
- **可观察结果**：手机端为移动布局（底部标签栏、卡片式周视图），电脑端为宽屏周视图；两端均可完成全部操作。

**数据字段说明**（所有日程条目通用）：名称（必填）、时间（必填）、备注（选填）；课程类另有周次规则与教室字段；待办另有截止时间与完成状态字段。

## 五、验收标准（逐条可检查）

> 检验方法：换一个不了解项目的人，按下面的步骤操作，每条结果必须一致。

| # | 验收条款 | 检查方式 |
|---|---|---|
| A1 | 完成学期设置后，周视图正确显示当前是第几周 | 设置开学日期为上周一，页面应显示「第 2 周」 |
| A2 | 添加一门「每周」课，课程出现在周视图对应格子 | 填名称+星期+节次，保存后格子可见课程名 |
| A3 | 添加一门「单周」课，它在单周显示、双周不显示 | 周次切换对比：第 1 周可见，第 2 周该格子为空 |
| A4 | 添加时间重叠的课程时出现冲突提示 | 两门课排在同一时段，第二条保存时弹出提示 |
| A5 | 添加一条待办，它出现在今日视图中 | 填标题+截止今天，保存后今日视图可见 |
| A6 | 打勾后待办视觉变化，再点可取消 | 勾选后变灰/划线，再点恢复 |
| A7 | 今日结算数字正确且实时更新 | 今日有 3 条到期待办，完成 1 条时显示「1/3」，再打勾一条变为「2/3」（课程不计入） |
| A8 | 关闭浏览器重开后数据仍在 | 全部录入 → 关闭浏览器 → 重开网址 → 课程、待办、打勾状态全部保留 |
| A9 | 导出 .ics 导入手机日历后，课前 15 分钟提醒生效 | 电脑导出 → 手机日历导入 → 到「上课前 15 分钟」时系统弹出通知 |
| A10 | 导出 JSON 后可在另一设备完整导入 | 电脑导出 JSON → 手机端导入 → 两端数据一致 |
| A11 | 手机与电脑均能完成「录入→查看→打勾」全流程 | 两台设备各操作一遍，无功能缺失或布局错乱 |

## 六、本期不做（边界清单）

> 与 research.md 第七节保持一致，此处只列结论；详细论证见 research.md。

**明确不做：**
- ❌ 教务系统自动导入 / 导入 .ics·Excel 课表文件（前者团队级维护成本，后者已实测排除）
- ❌ 用户账号体系、多用户、共享协作（纯自用）
- ❌ 番茄钟、习惯打卡、专注统计
- ❌ 原生 App / APK（本期为响应式 Web）

**本期不进入、留待后续期次（按方案分期）：**
- ⏳ 课表图片 AI 识别（一期后段加速入口，需先确认多模态 API key）
- ⏳ 云同步与多端互通（用户已定：非刚需，导出/导入作迁移兜底）
- ⏳ 课堂录音转写总结（二期，前置两周真实验证期）
- ⏳ 打卡与固定循环日程（三期）
- ⏳ 碎片练耳（四期）
- ⏳ AI 每日复盘（五期）
- ⏳ AI 自动排时间（排程质量与稳定性需实测后才可承诺，见 research.md 待核实第 7 条）

**技术边界**：本 PRD 不约定技术选型（框架、存储引擎、托管方式），相关决策属 Day 5 技术设计。

## 七、成功指标（自用版）

个人工具不以用户量衡量，改以真实使用衡量——对应方案「先验证再建设」原则：

1. 一期交付后，**真实使用不少于 2 周**（验证期）；
2. 每天至少打开一次今日视图；
3. 课表录入成本可接受：新学期 20 门以内课程，10 分钟内录完；
4. 验证期结束后回顾：哪些功能真在用、哪些没用——没用的功能在二期砍掉或搁置。
