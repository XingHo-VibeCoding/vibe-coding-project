# 大学生日程助手 —— 产品设计方案

> 版本：v1.1 ｜ 日期：2026-09-17 定稿 / 2026-09-18 修订 ｜ 性质：个人自用工具（非商业化）
> 决策记录：网页版优先（手机 + 电脑响应式访问）、轻量云同步、AI 能力全部接入、数据零成本运行为目标
>
> **v1.1 修订说明（2026-09-18）**：确立「规则优先，AI 兜底」为实现原则（见新增第一节之二）。一期课表录入由「AI 识别优先」调整为「手动录入优先，AI 识别作为后续补充的加速入口」，使一期不再阻塞于多模态 API key。**云同步由一期范围移出**（用户决策：当前非刚需，数据先存本地，云同步留待后续期次）。数据模型与分期结构不变。

---

## 一、项目定位

一款面向个人大学生用户的日程管理工具，核心闭环：

```
课表导入（计划）→ 提醒（执行）→ 录音/打卡/练耳（记录）→ AI 总结与待办（沉淀）→ 每日复盘（反思）
```

所有输入最终沉淀为待办与复盘，避免做成"功能堆砌型"课程表。

**设计三原则**

1. 无感易用：与当前任务无关的界面元素绝不出现
2. 数据自主：数据可导出，成本趋近于零，AI 调用可自助替换
3. 先验证再建设：每期上线前，必须验证上一期功能的真实使用情况

**实现原则：规则优先，AI 兜底**

能用确定性规则解决的问题，绝不交给 AI。判断标准：**这个决定的答案是不是唯一的？**

| 答案唯一 → 用规则 | 需要理解/判断 → 用 AI |
|---|---|
| 周次换算、单双周判断 | 课表图片识别 |
| 时间冲突检测 | 录音转写 |
| 提醒触发、今日统计 | AI 总结 / 每日复盘 |
| 手动录入课表与待办 | 复杂排程偏好（简单排程用规则） |

**理由**：规则的结果 100% 可预测、零成本、毫秒级；AI 灵活但有成本、有延迟、结果不稳定。典型反例：用 AI 解析课表文本或判断时间冲突——耗时数秒且时有出错，而本该毫秒级算准。

**因此**：一期的核心闭环（手动录入 → 规则解析 → 提醒 → 统计）全部可纯规则实现，不依赖任何 AI 服务；AI 能力按后续期次逐步接入，仅在「规则确实做不到」的场景使用（详见 research.md 第六节）。

---

## 二、功能全景

| 模块 | 说明 | 期数 |
|------|------|------|
| 课表录入 | **手动录入优先（规则）**；课表图片 → 多模态 AI 识别 → 确认页 → 入库，作为省录入的加速入口后续补充 | 一期（手动）/ 一期后段（AI 识别） |
| 日程管理 | 四类日程：课表课程 / 独立日程 / 每日打卡 / 固定循环日程 | 一期（打卡与循环日程三期） |
| 课前提醒 | 一期经"导入系统日历"实现；原生推送属二期后 | 一期 |
| 课堂录音总结 | 日程开始提醒 → 一键录音（连续采集、分片存储）→ 自动转写 → AI 总结 → 生成待办 | 二期 |
| 碎片练耳 | 自备英语音频（20s–1min），日程空闲槽提醒播放，按"重复遍数"调度 | 四期 |
| 每日复盘 | 每晚 23:00 汇总当日数据，AI 引导式对话生成日精进，产物可转待办 | 五期 |

> 周次/单双周解析、冲突检测、今日统计、提醒触发均以规则实现（见「实现原则」），不消耗 AI 调用。

---

## 三、数据模型（核心）

统一日程表，一套数据、四种视图（周视图 / 日视图 / 今日页 / 打卡页）。

### schedules（日程表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| type | enum | `course` 课表课程 / `event` 独立日程 / `habit` 每日打卡 / `routine` 固定循环 |
| title | string | 名称（课程名 / 事件名 / 习惯名） |
| location | string | 教室 / 地点（可空） |
| repeat_rule | object | 一次性：具体日期；循环：每周几；课程：星期 + 节次 + 周次规则（每周 / 单周 / 双周，一期够用） |
| start_time / duration | time | 开始时间、持续时长（打卡可为"建议时段"） |
| color / emoji | string | 视觉标识 |
| semester_id | string | 所属学期（课程类用，学期末整体归档） |
| manual_edited | bool | 用户手动调整过，重新导入课表时不覆盖 |

### 其他表

- **semesters**：学期名、第一周周一日期（周次换算锚点）、总周数、各节次起止时间表（`periods`，Day 8 新增；各校节次时间不同，做成可编辑模板而非写死，空则用默认 10 节）
- **todos**：待办（来源：手动 / 课堂总结提取 / 复盘提取），含截止时间、完成状态
- **habits_records**：打卡记录（habit_id + 日期），用于 streak 计算
- **lectures**：课堂录音场次（关联 schedule，状态机：录音中 → 转写中 → 已总结）
- **audio_clips**：练耳音频（文件名、已听次数、最后收听时间、归档状态）
- **reviews**：每日复盘存档（日期、原始对话、生成的日精进）

### 关键规则

- 重新导入课表 = 本学期 `course` 类型整体替换；`manual_edited=true` 的课程保留并标注
- 冲突检测：添加日程时检查时间重叠，提示但不强制阻止
- 数据导出：一键导出全部数据为 JSON（防丢、防平台绑定）

---

## 四、技术架构（第一期）

```
┌────────────────────────────────────────────┐
│ 响应式 Web App（同一代码库）                  │
│  移动端：底部标签栏 / 卡片周视图               │
│  桌面端：宽屏周视图 / 侧边栏                  │
├────────────┬───────────────┬───────────────┤
│ 云数据库    │ AI 中转服务    │ 静态托管        │
│（免费 BaaS）│（Cloudflare   │（Cloudflare    │
│  数据互通   │  Workers）    │  Pages 等）    │
└────────────┴───────────────┴───────────────┘
        │                │
        ▼                ▼
   多模态大模型      课表识别 / 总结 / 复盘
  （用户自有 key）
```

- **前端**：纯 Web（React/Vue 或原生均可，开发时定），PWA 支持"添加到主屏幕"
- **数据存储（一期）**：**浏览器本地存储（localStorage / IndexedDB）**，无后端依赖，打开即用
- **云同步（后续期次，一期不做）**：用户决策——当前非刚需，先本地可用。后续接入时优先使用 WorkBuddy 内置云服务（云数据库/托管）；若不可用则选腾讯云开发 / Supabase 免费档，设备码即可读写，不做完整账号体系。数据模型与导出功能为一期已有基础，后续接入不需重构
- **AI 调用**：网页 → 中转服务 → 多模态/文本大模型（解决浏览器 CORS）。优先使用 WorkBuddy 内置的免密钥 LLM 云服务；自行搭建时用 Cloudflare Workers 中转，key 存于中转服务配置
- **提醒（一期）**：手机端课表一键导出 `.ics` / 写入系统日历，由系统负责课前提醒；电脑端用浏览器通知辅助

---

## 五、分期计划与验收标准

### 一期：课表 + 日程 + 提醒（预计 2 周）

范围：学期设置、**手动课表录入（规则）**、确认页编辑、四类日程模型（先只开放课程与独立日程）、周/日视图、导入系统日历、数据导出（JSON）、响应式双端。**数据存本地，不含云同步**（用户决策：非刚需，移至后续期次）。课表图片 AI 识别作为后段加速入口（需多模态 API key，不阻塞一期主线）。

- 验收：手动录入一份真实课表可正常用于周视图与提醒；课表识别（后段）错误率可接受（确认页 5 分钟内纠错完成）；**关掉浏览器重开数据仍在**；课前提醒经系统日历稳定触发。
- **一期不依赖项**：多模态 API key（AI 识别为后段补充，主体功能不阻塞）

### 验证期（2 周，零开发）

用真实课表生活两周，同时：手机自带录音机 + 通义听悟/讯飞听见（免费额度）体验"录音→转写→总结"全流程。
- 决策点：课堂总结是否真有价值？转写质量是否可接受？**有价值才立项二期。**

### 二期：课堂录音总结（原生能力，PWA 受限时优先手机端）

范围：日程开始弹高优先级通知 → 一键开始录音；连续采集 + 10 分钟分片存储（AudioRecord 方案，不启停分段）；结束后自动转写（分片并行）→ 拼接 → LLM 总结并提取待办 → 完成通知。

- 技术要点：Android 前台服务（microphone 类型）、国产 ROM 白名单引导、转写首选本地 SenseVoice（sherpa-onnx，免费无限量），讯飞听写 500 次/日 + 各家一次性额度为云端备胎。
- 验收：一节课录音 → 总结 → 待办全链路自动完成，无需二次交互。

### 三期：打卡 + 循环日程

范围：每日打卡（streak、宽限期漏卡）、固定循环日程、冲突检测提示、日程分类视图。

### 四期：碎片练耳

范围：本地音频导入、日程空闲槽检测（短槽 1 段 / 长槽连播）、"通知 + 一键播放"、每段重复 3–5 遍的精听调度、勿扰时段优先。可先用 Tasker + 播放器搭土法版提前验证行为。

### 五期：每日复盘

范围：23:00 轻提醒 → 3–5 个引导问题的限时对话（2–3 分钟）→ 汇总当日日程/待办/打卡/练耳数据生成日精进 → 关键句一键转待办。进阶：接入 Android UsageStats 读取真实屏幕使用时长，作为复盘数据源。

- 纪律：每期上线前必须确认上一期功能仍在被真实使用，否则暂停扩张。

---

## 六、主要风险与对策

| 风险 | 等级 | 对策 |
|------|------|------|
| 范围膨胀烂尾 | 🔴 | 严格执行分期与验证期；backlog 之外的想法只记录不开发 |
| 单双周等课表规则解析出错 | 🔴 | **规则实现（非 AI）**：周次换算与单双周判断用确定性代码，避免 AI 出错；一期只支持"每周/单周/双周"三档 |
| AI 识别/接口未就绪导致阻塞 | 🟡 | **规则优先原则**：一期核心闭环（录入/解析/提醒/统计）不依赖 AI；AI 能力统一按后段补充，接口到位再接，不阻塞主线 |
| 国产 ROM 杀后台致提醒/录音失效 | 🔴 | 白名单引导流程；一期提醒走系统日历规避；录音用前台服务 |
| 教室录音质量差致转写不可用 | 🟡 | 验证期先行测试；选前排/麦克风授课课程优先录音 |
| 云数据库免费额度超限 | 🟢 | 个人数据量极小，基本不可能超；一期改用本地存储后此风险在一期不存在 |
| 云同步延迟或缺席导致换设备不可用 | 🟢 | 用户已定：一期不做云同步，本地可用即可；数据导出 JSON 功能可作迁移兜底 |
| 深夜复盘坚持不下来 | 🟡 | 对话限时 2–3 分钟、AI 主动提问；先手动用豆包/Kimi 土法验证一周 |

---

## 七、成本估算（月）

| 项目 | 费用 |
|------|------|
| 云数据库（免费档） | ¥0 |
| 静态托管 + 中转服务（免费档） | ¥0 |
| 课表识别 + 总结 + 复盘（少量 LLM 调用） | ¥0–5 |
| 课堂转写（本地 SenseVoice） | ¥0（云端备胎约 ¥30–90/月，仅作备用） |
| **合计** | **≈ ¥0–5 / 月** |

---

## 八、过渡期现成软件组合（自建完成前使用）

1. 日程提醒：系统日历手动录入或一期网页版导入
2. 录音转写总结：手机自带录音机（转写）或通义听悟 / 讯飞听见 → 文本丢给豆包/Kimi 总结提取待办
3. 碎片练耳：Tasker / MacroDroid 定时提醒 + 本地播放器
4. 每日复盘：固定 prompt + 豆包/Kimi 对话

---

## 九、开发交接说明（给执行开发的会话）

> 本节写给新的开发会话。产品决策均已定稿，按本方案执行即可，无需重新讨论方向。

1. **只做一期，不要实现全部功能。** 本文档第二、五节是完整 backlog（五期），一期范围以第五节"一期"清单为准，其余内容仅作架构预留（数据模型可先行建全，功能界面只做一期）。
2. **界面移动端优先**：先保证手机体验（底部标签栏、卡片式布局），桌面端做宽屏周视图适配即可。
3. **实现必须遵循「规则优先，AI 兜底」**（见第一节之二）：周次解析、冲突检测、提醒、统计一律用规则；AI 只用于图片识别、语音转写、语言总结三类场景。不得让 AI 承担答案唯一的计算任务。
4. **开工前需向用户确认的事项**：
   - 多模态大模型 API key（哪家、是否有免费额度）——课表图片识别所需，**AI 识别为后段补充，不阻塞一期主线**
   - ~~云同步走 WorkBuddy 内置云服务还是外部 BaaS~~ ——**已解决**：一期不做云同步，改用浏览器本地存储（2026-09-18 用户决策）
5. **验收标准**见第五节一期条目：手动录入课表可用于周视图与提醒、关掉浏览器重开数据仍在、课表可导出 `.ics` 写入系统日历。
6. **设计三原则必须遵守**（见第一节），尤其"无感易用"：与当前任务无关的功能不出现在界面上。

---

## 附：已明确的边界决策

- 纯自用，不做多用户、不做商业化、不建账号体系（仅设备码）
- 不自建词库体系：单词学习借助成熟 app，本工具只负责日程感知的提醒调度
- 第一期不做原生通知推送；原生 APK 迁移在二期引入（界面代码可复用）
- 录音涉及他人声音，仅限个人学习使用，不对外传播
- **不做教务系统对接**：WakeUp 的「3000+ 高校导入」实为每校一套「模拟登录+页面解析」规则（团队级维护成本），个人不可承担；亦不做 `.ics`/Excel 导入（已实测 CeleChron 无导出功能）。课表录入路径 = 手动录入（一期主线）+ 图片 AI 识别（后段加速入口）
